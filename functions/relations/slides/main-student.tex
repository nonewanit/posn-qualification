% !TEX program = xelatex
% ============================================================================
%  POSN Computer Qualification — Functions Slide Deck
%  Topic: ความสัมพันธ์และฟังก์ชัน (Relations and Functions)
% ============================================================================

\documentclass[11pt, aspectratio=169]{beamer}

% --- Load shared preamble ---
\input{../../../preamble.tex}

% --- Document info ---
\title[ความสัมพันธ์และฟังก์ชัน]{\textcolor{white}{ความสัมพันธ์และฟังก์ชัน} \\ \textcolor{boxBlue!60}{(Relations and Functions)}}
\subtitle{ความสัมพันธ์ • ฟังก์ชัน • ประเภทของฟังก์ชัน • คุณสมบัติของความสัมพันธ์ }
\author[None Wanitchollakit]{None W. (Yuan)}
\date{}

\begin{document}

% ===========================================================================
%  TITLE SLIDE
% ===========================================================================
\begin{frame}[plain]
  \titlepage
\end{frame}

% ===========================================================================
%  OUTLINE
% ===========================================================================
\begin{frame}{สารบัญ (Outline)}
  \tableofcontents
\end{frame}

% ===========================================================================
%  SECTION: บทนำ — ความสัมพันธ์ (Relations)
% ===========================================================================
\section{ความสัมพันธ์ (Relations)}

\begin{frame}{ความสัมพันธ์คืออะไร?}
  \begin{explanation}[นิยาม: ความสัมพันธ์ (Relation)]
    ให้ $A$ และ $B$ เป็นเซตใด ๆ \textbf{ความสัมพันธ์} $r$ จาก $A$ ไปยัง $B$
    คือ \highlight{เซตย่อยของผลคูณคาร์ทีเซียน} $A \times B$

    \medskip
    นั่นคือ $r \subseteq A \times B$

    \medskip
    ถ้า $(a, b) \in r$ เรากล่าวว่า $a$ \textbf{มีความสัมพันธ์} $r$ กับ $b$
    และเขียนแทนด้วย $a \; r \; b$
  \end{explanation}

  \begin{itemize}
    \item $A$ เรียกว่า \highlight{เซตตั้งต้น} (domain set)
    \item $B$ เรียกว่า \highlight{เซตปลายทาง} (codomain set)
  \end{itemize}

  \begin{exambox}[ตัวอย่าง]
    ความสัมพันธ์เหมือนกับการจับคู่ เช่น $r=\left\{(1,2),(1,-2),(3, 9)\right\} $ เป็นต้น
  \end{exambox}
\end{frame}\begin{frame}{แผนภาพ: การจับคู่ของความสัมพันธ์}
  \begin{columns}[T]
    \column{0.5\textwidth}
      \centering
      \begin{tikzpicture}[scale=0.9,
        elem/.style={circle, draw=boxBlue, thick, fill=white, inner sep=0pt, minimum size=18pt, font=\sffamily},
        elemno/.style={circle, draw=gray, thick, fill=gray!10, inner sep=0pt, minimum size=18pt, font=\sffamily},
        arrow/.style={->, >=stealth, thick, boxBlue, shorten >=2pt, shorten <=2pt}]
        % Set A (left oval)
        \draw[boxBlue!50, very thick] (0,0) ellipse (1.4cm and 2.0cm);
        \node[font=\sffamily\small] at (0,-2.6) {เซต $A$};
        % Set B (right oval)
        \draw[boxBlue!50, very thick] (4.5,0) ellipse (1.4cm and 2.0cm);
        \node[font=\sffamily\small] at (4.5,-2.6) {เซต $B$};
        % Elements A
        \node[elem] (a1) at (0,1.0) {1};
        \node[elem] (a2) at (0,0.0) {2};
        \node[elem] (a3) at (0,-1.0) {3};
        % Elements B
        \node[elem] (b4) at (4.5,1.0) {4};
        \node[elem] (b5) at (4.5,0.0) {5};
        \node[elemno] (b6) at (4.5,-1.0) {6};
        % Arrows
        \draw[arrow] (a1) -- (b4);
        \draw[arrow] (a2) -- (b5);
        \draw[arrow] (a3) -- (b4);
        % Label
        \node[font=\sffamily\small] at (2.25,-3.3) {$r = \{(1,4), (2,5), (3,4)\}$};
      \end{tikzpicture}

    \column{0.5\textwidth}
      \begin{intuition}[หลักการ]
        \begin{itemize}
          \item \textbf{ลูกศรแต่ละเส้น} = 1 คู่อันดับใน $r$
          \item ความสัมพันธ์ $r$ = \highlight{เซตของลูกศร}ทั้งหมด
        \end{itemize}
      \end{intuition}
  \end{columns}
\end{frame}\begin{frame}{ผลคูณคาร์ทีเซียน (Cartesian Product)}
  \begin{explanation}[นิยาม: ผลคูณคาร์ทีเซียน]
    \textbf{ผลคูณคาร์ทีเซียน} ของเซต $A$ และ $B$ คือเซตของคู่อันดับ $(a, b)$ ทั้งหมด
    โดยที่ $a \in A$ และ $b \in B$

    \medskip
    $A \times B = \{(a, b) \mid a \in A \text{ และ } b \in B\}$
  \end{explanation}

  \begin{exambox}[ตัวอย่าง]
    ให้ $A = \{1, 2\}$ และ $B = \{a, b, c\}$

    \medskip
    $A \times B = \{(1,a), (1,b), (1,c), (2,a), (2,b), (2,c)\}$

    \medskip
    $|A \times B| = |A| \times |B| = 2 \times 3 = 6$
  \end{exambox}
\end{frame}\begin{frame}{โดเมนและเรนจ์ของความสัมพันธ์}
  \begin{explanation}[นิยาม]
    ให้ $r$ เป็นความสัมพันธ์จาก $A$ ไปยัง $B$

    \medskip
    \textbf{โดเมน} (Domain) ของ $r$:
    \quad $D_r = \{a \in A \mid \text{มี } b \in B \text{ ที่ } (a,b) \in r\}$

    \medskip
    \textbf{เรนจ์} (Range) ของ $r$:
    \quad $R_r = \{b \in B \mid \text{มี } a \in A \text{ ที่ } (a,b) \in r\}$
  \end{explanation}

  \begin{itemize}
    \item โดเมน $\subseteq A$ — เฉพาะสมาชิกของ $A$ ที่ถูกใช้
    \item เรนจ์ $\subseteq B$ — เฉพาะสมาชิกของ $B$ ที่ถูกใช้
  \end{itemize}

  \begin{intuition}[สังเกต]
    ง่าย ๆ โดเมน คือ ตัวหน้าของคู่ ($a$) เป็นอะไรได้บ้าง เรนจ์ คือ ตัวหน้าของคู่ ($b$) เป็นอะไรได้บ้าง
  \end{intuition}
\end{frame}\begin{frame}{แผนภาพ: โดเมนและเรนจ์}
  \begin{columns}[T]
    \column{0.5\textwidth}
      \centering
      \begin{tikzpicture}[scale=0.9,
        elemdom/.style={circle, draw=boxBlue, thick, fill=boxBlue!15, inner sep=0pt, minimum size=18pt, font=\sffamily},
        elemran/.style={circle, draw=boxGreen, thick, fill=boxGreen!15, inner sep=0pt, minimum size=18pt, font=\sffamily},
        elemno/.style={circle, draw=gray, thick, fill=gray!10, inner sep=0pt, minimum size=18pt, font=\sffamily},
        arrow/.style={->, >=stealth, thick, boxBlue, shorten >=2pt, shorten <=2pt}]
        % Set A (left oval)
        \draw[boxBlue!50, very thick] (0,0) ellipse (1.4cm and 2.0cm);
        \node[font=\sffamily\small] at (0,-2.6) {เซต $A$};
        % Set B (right oval)
        \draw[boxBlue!50, very thick] (4.5,0) ellipse (1.4cm and 2.0cm);
        \node[font=\sffamily\small] at (4.5,-2.6) {เซต $B$};
        % Domain elements (blue filled) — all have outgoing arrows
        \node[elemdom] (a1) at (0,1.0) {1};
        \node[elemdom] (a2) at (0,0.0) {2};
        \node[elemdom] (a3) at (0,-1.0) {3};
        % Range elements (green filled) — have incoming arrows
        \node[elemran] (b4) at (4.5,1.0) {4};
        \node[elemran] (b5) at (4.5,0.0) {5};
        % Not in range (gray)
        \node[elemno] (b6) at (4.5,-1.0) {6};
        % Arrows
        \draw[arrow] (a1) -- (b4);
        \draw[arrow] (a2) -- (b5);
        \draw[arrow] (a3) -- (b4);
        % Labels
        \node[font=\sffamily\small\bfseries, boxBlue] at (0,-3.3) {$D_r = \{1,2,3\}$};
        \node[font=\sffamily\small\bfseries, boxGreen] at (4.5,-3.3) {$R_r = \{4,5\}$};
        % Legend
        \node[elemdom, label=right:{\small โดเมน}] at (-1.8,2.8) {};
        \node[elemran, label=right:{\small เรนจ์}] at (0.2,2.8) {};
        \node[elemno, label=right:{\small ไม่อยู่ในเรนจ์}] at (2.2,2.8) {};
      \end{tikzpicture}

    \column{0.45\textwidth}
      \begin{intuition}[จำง่าย ๆ]
        \begin{itemize}
          \item \textbf{โดเมน} = สมาชิกใน $A$ ที่\textbf{มีลูกศรออก}
          \item \textbf{เรนจ์} = สมาชิกใน $B$ ที่\textbf{ถูกลูกศรชี้}
          \item $6$ อยู่ใน $B$ แต่ไม่มีลูกศรชี้ $\rightarrow$ ไม่อยู่ในเรนจ์
        \end{itemize}
      \end{intuition}
  \end{columns}
\end{frame}\begin{frame}[t]{ตัวอย่าง: การหาโดเมนและเรนจ์}
  \begin{exambox}[โจทย์]
    กำหนด $r = \{(x, y) \mid y = x^2,\; x \in \{-2, -1, 0, 1, 2\}\}$
    จงหา $D_r$ และ $R_r$
  \end{exambox}

  \vspace{\fill}

  \medskip
  \begin{intuition}[สังเกต]
    โดเมนคือค่า $x$ ที่ใส่เข้าไป เรนจ์คือค่า $y$ ที่ได้ออกมา
  \end{intuition}

\end{frame}\begin{frame}[t]{ตัวอย่าง: ข้อสอบจริง}
  \begin{exambox}[โจทย์ปี 68 ข้อ 10]
    กำหนด $A=\{1,2,3\}, B=\{4,5\}$ และความสัมพันธ์ $r$ จากเซต $A$ ไปยังเซต $B$ โดย $r=\{(a,b)\in A\times B\mid a+b$ เป็นจำนวนคี่ $\}$ ข้อใดเป็นจริง \newline
    [ข. $r$ มีสมาชิก 6 คู่อันดับ]
    [ง. $r$ ไม่มีสมาชิกเลย]
  \end{exambox}

  \vspace{\fill}

\end{frame}

% ===========================================================================
%  SECTION: ฟังก์ชัน (Functions)
% ===========================================================================
\section{ฟังก์ชัน (Functions)}

\begin{frame}{ฟังก์ชันคืออะไร?}
  \begin{explanation}[นิยาม: ฟังก์ชัน (Function)]
    \textbf{ฟังก์ชัน} คือความสัมพันธ์ที่สมาชิกแต่ละตัวในโดเมน
    \highlight{จับคู่กับสมาชิกในเรนจ์เพียงตัวเดียวเท่านั้น}

    \medskip
    นั่นคือ ถ้า $(a, b_1) \in f$ และ $(a, b_2) \in f$ แล้ว $b_1 = b_2$
  \end{explanation}

  \begin{itemize}
    \item ฟังก์ชันเป็น \highlight{ความสัมพันธ์ชนิดพิเศษ}
    \item ทุกฟังก์ชันเป็นความสัมพันธ์ แต่ไม่ทุกความสัมพันธ์เป็นฟังก์ชัน
    \item เขียนแทนด้วย $f: A \to B$ หมายถึง $f$ เป็นฟังก์ชันจาก $A$ ไป $B$
  \end{itemize}

  \begin{intuition}[สังเกต]
    ง่าย ๆ ฟังก์ชัน คือ ความสัมพันธ์ที่จาก $x$ จับคู่ได้กับ $y$ ตัวเดียว เช่น $r = \left\{(x,y) \mid x = \left\lvert y\right\rvert \right\} $ ไม่ใช่ฟังก์ชัน เพราะ 1 มีความสัมพันธ์ $r$ กับทั้ง 1 และ -1
  \end{intuition}
\end{frame}\begin{frame}{แผนภาพ: ฟังก์ชัน vs ไม่ใช่ฟังก์ชัน}
  \begin{columns}[T]
    \column{0.48\textwidth}
      \centering
      % \textbf{เป็นฟังก์ชัน $\checkmark$}
      \begin{tikzpicture}[scale=0.82,
        elem/.style={circle, draw, thick, fill=white, inner sep=0pt, minimum size=17pt, font=\sffamily},
        arr/.style={->, >=stealth, very thick, boxGreen, shorten >=2pt, shorten <=2pt}]
        \draw[boxGreen!40, very thick] (0,0) ellipse (1.1cm and 1.8cm);
        \node[font=\sffamily\small] at (0,-2.3) {$A$};
        \draw[boxGreen!40, very thick] (3.5,0) ellipse (1.1cm and 1.8cm);
        \node[font=\sffamily\small] at (3.5,-2.3) {$B$};
        \node[elem] at (0,0.9) {1};
        \node[elem] at (0,0.0) {2};
        \node[elem] at (0,-0.9) {3};
        \node[elem] at (3.5,0.9) {4};
        \node[elem] at (3.5,0.0) {5};
        \node[elem] at (3.5,-0.9) {6};
        \draw[arr] (0.22,0.9) -- (3.28,0.9);
        \draw[arr] (0.22,0.0) -- (3.28,0.0);
        \draw[arr] (0.22,-0.9) -- (3.28,0.7);
        \node[boxGreen, font=\sffamily\footnotesize, align=center] at (1.75,-3.1)
          {สมาชิกใน $A$ แต่ละตัว\\มีลูกศรออก\textbf{เพียง 1 เส้น}};
      \end{tikzpicture}

    \column{0.48\textwidth}
      \centering
      % \textbf{ไม่เป็นฟังก์ชัน $\times$}
      \begin{tikzpicture}[scale=0.82,
        elem/.style={circle, draw, thick, fill=white, inner sep=0pt, minimum size=17pt, font=\sffamily},
        elembad/.style={circle, draw=red!70, thick, fill=red!10, inner sep=0pt, minimum size=17pt, font=\sffamily},
        arr/.style={->, >=stealth, very thick, red!70, shorten >=2pt, shorten <=2pt}]
        \draw[red!30, very thick] (0,0) ellipse (1.1cm and 1.8cm);
        \node[font=\sffamily\small] at (0,-2.3) {$A$};
        \draw[red!30, very thick] (3.5,0) ellipse (1.1cm and 1.8cm);
        \node[font=\sffamily\small] at (3.5,-2.3) {$B$};
        \node[elembad] at (0,0.9) {1};
        \node[elem] at (0,0.0) {2};
        \node[elem] at (0,-0.9) {3};
        \node[elem] at (3.5,0.9) {4};
        \node[elem] at (3.5,0.0) {5};
        \node[elem] at (3.5,-0.9) {6};
        \draw[arr] (0.22,0.9) -- (3.28,0.9);
        \draw[arr] (0.22,1.1) -- (3.28,-0.2);
        \draw[arr] (0.22,0.0) -- (3.28,-1.1);
        \node[red, font=\sffamily\footnotesize, align=center] at (1.75,-3.1)
          {$1$ มีลูกศรออก \textbf{2 เส้น}!\\ไม่ใช่ฟังก์ชัน};
      \end{tikzpicture}
  \end{columns}

  \vspace{0.3cm}
  \begin{center}
    \highlight{ฟังก์ชัน = ความสัมพันธ์ที่แต่ละ input มี output เพียงค่าเดียว}
  \end{center}
\end{frame}\begin{frame}{การทดสอบว่าเป็นฟังก์ชันหรือไม่}
  \begin{explanation}[การทดสอบแนวดิ่ง (Vertical Line Test)]
    กราฟจะเป็นกราฟของฟังก์ชัน ก็ต่อเมื่อ
    \highlight{เส้นตรงแนวดิ่งทุกเส้นตัดกราฟได้ไม่เกิน 1 จุด}
  \end{explanation}

  \begin{columns}[T]
    \column{0.45\textwidth}
      \centering
      \textbf{เป็นฟังก์ชัน} \\[1em]
      \begin{tikzpicture}[scale=0.8]
        \draw[->] (-2,0) -- (2,0) node[right] {$x$};
        \draw[->] (0,-1) -- (0,3) node[above] {$y$};
        \draw[domain=-2:2, smooth, thick, boxBlue] plot (\x, {0.5*\x*\x});
        \draw[dashed, red] (1, -0.5) -- (1, 2.5);
      \end{tikzpicture}
      \par เส้นแนวดิ่งตัดกราฟ 1 จุด $\checkmark$

    \column{0.45\textwidth}
      \centering
      \textbf{ไม่เป็นฟังก์ชัน} \\[1em]
      \begin{tikzpicture}[scale=0.8]
        \draw[->] (-1,0) -- (3,0) node[right] {$x$};
        \draw[->] (0,-2) -- (0,2) node[above] {$y$};
        \draw[domain=0:3, smooth, thick, red] plot (\x, {sqrt(\x)});
        \draw[domain=0:3, smooth, thick, red] plot (\x, {-sqrt(\x)});
        \draw[dashed, red] (2, -2) -- (2, 2);
      \end{tikzpicture}
      \par เส้นแนวดิ่งตัดกราฟ 2 จุด $\times$
  \end{columns}
\end{frame}\begin{frame}[t]{ตัวอย่าง: ข้อสอบจริง}
  \begin{exambox}[โจทย์ปี 68 ข้อ 10]
    กำหนด $A=\{1,2,3\}, B=\{4,5\}$ และความสัมพันธ์ $r$ จากเซต $A$ ไปยังเซต $B$ โดย $r=\{(a,b)\in A\times B\mid a+b$ เป็นจำนวนคี่ $\}$ ข้อใดเป็นจริง \newline
    [ก. $r$ เป็นฟังก์ชัน]
  \end{exambox}

  \vspace{\fill}

\end{frame}\begin{frame}{สัญกรณ์ฟังก์ชัน (Function Notation)}
  \begin{explanation}[สัญกรณ์]
    $f: A \to B$ กำหนดโดย $f(x) = \text{นิพจน์ของ } x$

    \medskip
    \begin{itemize}
      \item $f$ คือ \highlight{ชื่อฟังก์ชัน}
      \item $x$ คือ \highlight{ตัวแปรอิสระ} (input)
      \item $f(x)$ คือ \highlight{ค่าของฟังก์ชันที่ $x$} (output)
      \item $A$ คือ \highlight{โดเมน}, $B$ คือ \highlight{โคโดเมน}
    \end{itemize}
  \end{explanation}
\end{frame}\begin{frame}[t]{ตัวอย่าง: การหาค่าฟังก์ชัน}
  \begin{exambox}[โจทย์]
    กำหนด $f(x) = x^2 + 3x - 1$ จงหา $f(2)$ และ $f(-1)$
  \end{exambox}

  \begin{intuition}[สังเกต]
    ฟังก์ชันเหมือน \textbf{เครื่องผลิตนํ้าผลไม้}: \newline
    ใส่ส้ม ($x$) เข้าไป $\rightarrow$ ได้นํ้าส้ม ($f(x)$) ออกมา\newline 
    ใส่แอปเปิ้ล เข้าไป $\rightarrow$ ได้นํ้าแอปเปิ้ล ออกมา
  \end{intuition}

  \vspace{\fill}

\end{frame}\begin{frame}[t]{โดเมนและเรนจ์ของฟังก์ชัน}
  \begin{exambox}[โจทย์]
    จงหาโดเมนของ $f(x) = \dfrac{1}{x - 3}$
  \end{exambox}

  \begin{itemize}
    \item \textbf{โดเมนของ $f$} คือเซตของค่า $x$ ทั้งหมดที่ทำให้ $f(x)$ หาค่าได้
    \item \textbf{เรนจ์ของ $f$} คือเซตของค่า $f(x)$ ทั้งหมดที่เกิดขึ้นจากทุก $x$ ในโดเมน
  \end{itemize}

  \vspace{\fill}

\end{frame}\begin{frame}[t]{ตัวอย่าง: การหาเรนจ์}
  \begin{exambox}[โจทย์]
    กำหนด $f(x) = x^2 + 2$ จงหาเรนจ์ของ $f$
  \end{exambox}

  \vspace{\fill}

\end{frame}\begin{frame}{เทคนิคการหาโดเมนและเรนจ์เบื้องต้น}
  \begin{explanation}[โดเมน]
    \begin{itemize}
      \item ต้องไม่ทำให้ส่วนเป็น 0
      \item ต้องไม่ทำให้ค่าใน square root ติดลบ
    \end{itemize}
  \end{explanation}
  \begin{explanation}[เรนจ์]
    \begin{itemize}
      \item ค่าที่ออกจาก square root จะมากกว่าหรือเท่ากับ 0
      \item ค่าที่ออกจาก absolute จะมากกว่าหรือเท่ากับ 0
      \item ค่าที่ออกจากการยกกำลังด้วยเลขคู่ จะมากกว่าหรือเท่ากับ 0
    \end{itemize}
  \end{explanation}
\end{frame}\begin{frame}[t]{ตัวอย่าง: ข้อสอบจริง}
  \begin{exambox}[โจทย์ปี 68 ข้อ 11]
    กำหนด ฟังก์ชัน $f: \mathbb{R} \rightarrow \mathbb{R}$ โดย $f(x)=\frac{2x+3}{x-1}$ ข้อใดกล่าว\textbf{ไม่ถูกต้อง} \newline
    [ก. โดเมนของ $f$ คือ $\mathbb{R} - \{1\}]$
  \end{exambox}

  \vspace{\fill}

\end{frame}

% ===========================================================================
%  SECTION: ประเภทของฟังก์ชัน (Types of Functions)
% ===========================================================================
\section{ประเภทของฟังก์ชัน (Types of Functions)}

\begin{frame}{ฟังก์ชันหนึ่งต่อหนึ่ง (One-to-One / Injective)}
  \begin{explanation}[นิยาม]
    ฟังก์ชัน $f: A \to B$ เป็นฟังก์ชัน \highlight{หนึ่งต่อหนึ่ง} (1-1) ก็ต่อเมื่อ

    \medskip
    \textbf{สมาชิกแต่ละตัวในโดเมนจับคู่กับสมาชิกในเรนจ์ที่แตกต่างกัน}

    \medskip
    ถ้า $f(a_1) = f(a_2)$ แล้ว $a_1 = a_2$
    (หรือใช้ข้อความแย้งสลับที่: ถ้า $a_1 \neq a_2$ แล้ว $f(a_1) \neq f(a_2)$)
  \end{explanation}

  \begin{itemize}
    \item ทดสอบด้วย \textbf{Horizontal Line Test}:
    เส้นแนวนอนทุกเส้นตัดกราฟได้ \highlight{ไม่เกิน 1 จุด}
  \end{itemize}

  \begin{intuition}[สังเกต]
    ง่าย ๆ คือห้ามคู่ใครซํ้ากับคู่อื่น
  \end{intuition}
\end{frame}\begin{frame}{แผนภาพ: ฟังก์ชันหนึ่งต่อหนึ่ง (1-1)}
  \centering
  \begin{tikzpicture}[scale=0.9,
    elem/.style={circle, draw, thick, fill=white, inner sep=0pt, minimum size=18pt, font=\sffamily},
    elembad/.style={circle, draw=red!70, thick, fill=red!10, inner sep=0pt, minimum size=18pt, font=\sffamily},
    arr/.style={->, >=stealth, very thick, shorten >=2pt, shorten <=2pt}]
    % === 1-1 ✓ (left) ===
    \node[font=\sffamily\small\bfseries, boxGreen] at (2,2.8) {หนึ่งต่อหนึ่ง $\checkmark$};
    \draw[boxGreen!30, very thick] (0,0) ellipse (1.2cm and 2.0cm);
    \node[font=\sffamily\small] at (0,-2.5) {$A$};
    \draw[boxGreen!30, very thick] (3.8,0) ellipse (1.2cm and 2.0cm);
    \node[font=\sffamily\small] at (3.8,-2.5) {$B$};
    \node[elem] (a1) at (0,1.0) {1};
    \node[elem] (a2) at (0,0.0) {2};
    \node[elem] (a3) at (0,-1.0) {3};
    \node[elem] (b1) at (3.8,1.3) {4};
    \node[elem] (b2) at (3.8,0.0) {5};
    \node[elem] (b3) at (3.8,-1.3) {6};
    \draw[arr, boxGreen] (a1) -- (b1);
    \draw[arr, boxGreen] (a2) -- (b3);
    \draw[arr, boxGreen] (a3) -- (b2);
    \node[boxGreen, font=\sffamily\footnotesize] at (1.9,-3.2) {แต่ละตัวใน $B$ ถูกลูกศรชี้ไม่เกิน 1 ครั้ง};

    % Separator
    \draw[gray!50, thick] (5.5,-2.0) -- (5.5,2.8);

    % === NOT 1-1 ✗ (right) ===
    \node[font=\sffamily\small\bfseries, red] at (9.0,2.8) {ไม่ 1-1 $\times$};
    \draw[red!30, very thick] (7.0,0) ellipse (1.2cm and 2.0cm);
    \node[font=\sffamily\small] at (7.0,-2.5) {$A$};
    \draw[red!30, very thick] (10.8,0) ellipse (1.2cm and 2.0cm);
    \node[font=\sffamily\small] at (10.8,-2.5) {$B$};
    \node[elem] (na1) at (7.0,1.0) {1};
    \node[elem] (na2) at (7.0,0.0) {2};
    \node[elem] (na3) at (7.0,-1.0) {3};
    \node[elembad] (nb1) at (10.8,1.3) {4};
    \node[elem] (nb2) at (10.8,-0.3) {5};
    \node[elem] (nb3) at (10.8,-1.3) {6};
    \draw[arr, red!70] (na1) -- (nb1);
    \draw[arr, red!70] (na2) -- (nb1);
    \draw[arr, red!70] (na3) -- (nb2);
    \node[red, font=\sffamily\footnotesize] at (8.9,-3.2) {$4$ โดนชี้ 2 ครั้ง $\rightarrow$ ไม่ 1-1};
  \end{tikzpicture}
\end{frame}\begin{frame}{ฟังก์ชันทั่วถึง (Onto / Surjective)}
  \begin{explanation}[นิยาม]
    ฟังก์ชัน $f: A \to B$ เป็นฟังก์ชัน \highlight{ทั่วถึง} (Onto) ก็ต่อเมื่อ

    \medskip
    \textbf{ทุกสมาชิกใน $B$ ถูกจับคู่โดยสมาชิกอย่างน้อยหนึ่งตัวใน $A$}

    \medskip
    $R_f = B$ (เรนจ์เท่ากับโคโดเมนทั้งหมด)
  \end{explanation}

  \begin{itemize}
    \item กล่าวอีกนัย: สำหรับทุก $b \in B$ จะต้องมี $a \in A$ ที่ $f(a) = b$
    \item ถ้าเรนจ์เป็นสับเซตแท้ของโคโดเมน จะไม่ใช่ฟังก์ชันทั่วถึง
  \end{itemize}

  \begin{intuition}[สังเกต]
    ง่าย ๆ คือ ฝั่งโคโดเมนต้องมีคู่ทั้งหมด
  \end{intuition}
\end{frame}\begin{frame}{แผนภาพ: ฟังก์ชันทั่วถึง (Onto)}
  \centering
  \begin{tikzpicture}[scale=0.9,
    elem/.style={circle, draw, thick, fill=white, inner sep=0pt, minimum size=18pt, font=\sffamily},
    elemused/.style={circle, draw=boxGreen, thick, fill=boxGreen!15, inner sep=0pt, minimum size=18pt, font=\sffamily},
    elemnone/.style={circle, draw=gray, thick, fill=gray!10, inner sep=0pt, minimum size=18pt, font=\sffamily},
    arr/.style={->, >=stealth, very thick, shorten >=2pt, shorten <=2pt}]
    % === ONTO ✓ (left) ===
    \node[font=\sffamily\small\bfseries, boxGreen] at (2,2.8) {ทั่วถึง $\checkmark$};
    \draw[boxGreen!30, very thick] (0,0) ellipse (1.2cm and 2.0cm);
    \node[font=\sffamily\small] at (0,-2.5) {$A$};
    \draw[boxGreen!30, very thick] (3.8,0) ellipse (1.2cm and 2.0cm);
    \node[font=\sffamily\small] at (3.8,-2.5) {$B$};
    \node[elem] (a1) at (0,1.1) {1};
    \node[elem] (a2) at (0,0.2) {2};
    \node[elem] (a3) at (0,-0.7) {3};
    \node[elem] (a4) at (0,-1.6) {4};
    \node[elemused] (b1) at (3.8,1.3) {5};
    \node[elemused] (b2) at (3.8,-0.3) {6};
    \node[elemused] (b3) at (3.8,-1.3) {7};
    \draw[arr, boxGreen] (a1) -- (b1);
    \draw[arr, boxGreen] (a2) -- (b2);
    \draw[arr, boxGreen] (a3) -- (b3);
    \draw[arr, boxGreen] (a4) -- (b2);
    \node[boxGreen, font=\sffamily\footnotesize] at (1.9,-3.3) {ทุกสมาชิกใน $B$ ถูกลูกศรชี้ (แต่ละตัวใน $A$ มี 1 ลูกศร)};

    % Separator
    \draw[gray!50, thick] (5.5,-2.0) -- (5.5,2.8);

    % === NOT ONTO ✗ (right) ===
    \node[font=\sffamily\small\bfseries, red] at (9.0,2.8) {ไม่ทั่วถึง $\times$};
    \draw[red!30, very thick] (7.0,0) ellipse (1.2cm and 2.0cm);
    \node[font=\sffamily\small] at (7.0,-2.5) {$A$};
    \draw[red!30, very thick] (10.8,0) ellipse (1.2cm and 2.0cm);
    \node[font=\sffamily\small] at (10.8,-2.5) {$B$};
    \node[elem] (na1) at (7.0,1.0) {1};
    \node[elem] (na2) at (7.0,0.0) {2};
    \node[elem] (na3) at (7.0,-1.0) {3};
    \node[elemused, draw=red!70, fill=red!10] (nb1) at (10.8,1.3) {4};
    \node[elemused, draw=red!70, fill=red!10] (nb2) at (10.8,-0.3) {5};
    \node[elemnone] (nb3) at (10.8,-1.3) {6};
    \draw[arr, red!70] (na1) -- (nb1);
    \draw[arr, red!70] (na2) -- (nb1);
    \draw[arr, red!70] (na3) -- (nb2);
    \node[red, font=\sffamily\footnotesize] at (8.9,-3.3) {$6$ ไม่มีลูกศรชี้ $\rightarrow$ ไม่ Onto};
  \end{tikzpicture}
\end{frame}\begin{frame}{ฟังก์ชันหนึ่งต่อหนึ่งและทั่วถึง (Bijective)}
  \begin{explanation}[นิยาม]
    ฟังก์ชัน $f: A \to B$ เป็นฟังก์ชัน \highlight{สมนัยหนึ่งต่อหนึ่ง} (Bijective) ก็ต่อเมื่อ

    \medskip
    $f$ เป็นทั้งฟังก์ชัน \textbf{หนึ่งต่อหนึ่ง (1-1)} และ \textbf{ทั่วถึง (Onto)}
  \end{explanation}

  \begin{itemize}
    \item ฟังก์ชัน bijective จะมี \textbf{ฟังก์ชันผกผัน} ($f^{-1}$) เสมอ
    \item $|D_f| = |B|$ สำหรับฟังก์ชัน bijective บนเซตจำกัด
  \end{itemize}

  \begin{intuition}[สังเกต]
    ง่าย ๆ คือ ฝั่งโคโดเมนต้องมีคู่ทั้งหมด และคู่ใครคู่มันห้ามซํ้า
  \end{intuition}
\end{frame}\begin{frame}{แผนภาพ: ฟังก์ชันสมนัยหนึ่งต่อหนึ่ง (Bijective)}
  \centering
  \begin{tikzpicture}[scale=0.9,
    elem/.style={circle, draw, thick, fill=white, inner sep=0pt, minimum size=18pt, font=\sffamily},
    arr/.style={->, >=stealth, very thick, boxBlue, shorten >=2pt, shorten <=2pt}]
    % Set A
    \draw[boxBlue!50, very thick] (0,0) ellipse (1.3cm and 2.0cm);
    \node[font=\sffamily\small\bfseries] at (0,-2.6) {$A$};
    % Set B
    \draw[boxBlue!50, very thick] (4.5,0) ellipse (1.3cm and 2.0cm);
    \node[font=\sffamily\small\bfseries] at (4.5,-2.6) {$B$};
    % Elements
    \node[elem] (a1) at (0,1.0) {1};
    \node[elem] (a2) at (0,0.0) {2};
    \node[elem] (a3) at (0,-1.0) {3};
    \node[elem] (b1) at (4.5,1.0) {4};
    \node[elem] (b2) at (4.5,0.0) {5};
    \node[elem] (b3) at (4.5,-1.0) {6};
    % One-to-one pairing: each A→different B, every B used
    \draw[arr] (a1) -- (b1);
    \draw[arr] (a2) -- (b3);
    \draw[arr] (a3) -- (b2);
    % Checkmarks
    \node[font=\sffamily\small, boxGreen, align=center] at (2.25,-3.4)
      {$\checkmark$ หนึ่งต่อหนึ่ง (1-1) \quad $\checkmark$ ทั่วถึง (Onto)};
    \node[font=\sffamily\small\bfseries, boxBlue] at (2.25,2.5)
      {$\Rightarrow$ มีฟังก์ชันผกผัน $f^{-1}$ ได้};
  \end{tikzpicture}
\end{frame}\begin{frame}{การดำเนินการของฟังก์ชัน}
  \begin{explanation}[นิยาม]
    กำหนด $f$ และ $g$ เป็นฟังก์ชันที่มีโดเมนร่วมกัน
  \end{explanation}

  \begin{align*}
    (f + g)(x) &= f(x) + g(x) &&\text{ผลบวก} \\
    (f - g)(x) &= f(x) - g(x) &&\text{ผลลบ} \\
    (f \cdot g)(x) &= f(x) \cdot g(x) &&\text{ผลคูณ} \\
    \left(\frac{f}{g}\right)(x) &= \frac{f(x)}{g(x)}, \; g(x) \neq 0 &&\text{ผลหาร}
  \end{align*}
\end{frame}\begin{frame}{ฟังก์ชันประกอบ (Composite Functions)}
  \begin{explanation}[นิยาม]
    กำหนด $f: A \to B$ และ $g: B \to C$

    \medskip
    \textbf{ฟังก์ชันประกอบ} $g \circ f$ (อ่านว่า ``$g$ composed with $f$'' หรือ ``$g$ o $f$'') คือ

    \medskip
    $(g \circ f)(x) = g(f(x))$

    \medskip
    \textbf{หมายเหตุ:} $R_f \subseteq D_g$ จึงจะหา $g \circ f$ ได้
  \end{explanation}

  \begin{exambox}[ตัวอย่าง]
    กำหนด $f(x) = 2x + 1$ และ $g(x) = x^2$

    \medskip
    $(g \circ f)(x) = g(f(x)) = g(2x + 1) = (2x + 1)^2 = 4x^2 + 4x + 1$

    $(f \circ g)(x) = f(g(x)) = f(x^2) = 2x^2 + 1$
  \end{exambox}
\end{frame}\begin{frame}{แผนภาพ: ฟังก์ชันประกอบ}
  \centering
  \begin{tikzpicture}[scale=0.85,
    elem/.style={circle, draw, thick, fill=white, inner sep=0pt, minimum size=18pt, font=\sffamily},
    arrf/.style={->, >=stealth, very thick, boxBlue, shorten >=2pt, shorten <=2pt},
    arrg/.style={->, >=stealth, very thick, boxGreen, shorten >=2pt, shorten <=2pt},
    arrgf/.style={->, >=stealth, very thick, boxOrange, shorten >=3pt, shorten <=3pt, dashed, bend left=35}]
    % Set A (left)
    \draw[boxBlue!50, very thick] (0,0) ellipse (1.2cm and 2.2cm);
    \node[font=\sffamily\small\bfseries] at (0,-2.8) {$A$};
    % Set B (center)
    \draw[boxGreen!50, very thick] (4,0) ellipse (1.2cm and 2.2cm);
    \node[font=\sffamily\small\bfseries] at (4,-2.8) {$B$};
    % Set C (right)
    \draw[boxOrange!50, very thick] (8,0) ellipse (1.2cm and 2.2cm);
    \node[font=\sffamily\small\bfseries] at (8,-2.8) {$C$};
    % Elements A
    \node[elem] (a1) at (0,1.2) {1};
    \node[elem] (a2) at (0,0.0) {2};
    \node[elem] (a3) at (0,-1.2) {3};
    % Elements B
    \node[elem] (b1) at (4,1.2) {$a$};
    \node[elem] (b2) at (4,0.0) {$b$};
    \node[elem] (b3) at (4,-1.2) {$c$};
    % Elements C
    \node[elem] (c1) at (8,1.2) {$x$};
    \node[elem] (c2) at (8,0.0) {$y$};
    \node[elem] (c3) at (8,-1.2) {$z$};
    % f: A→B (blue)
    \draw[arrf] (a1) -- (b1) node[midway, above] {\footnotesize $f$};
    \draw[arrf] (a2) -- (b2) node[midway, above] {\footnotesize $f$};
    \draw[arrf] (a3) -- (b3) node[midway, above] {\footnotesize $f$};
    % g: B→C (green)
    \draw[arrg] (b1) -- (c1) node[midway, above] {\footnotesize $g$};
    \draw[arrg] (b2) -- (c2) node[midway, above] {\footnotesize $g$};
    \draw[arrg] (b3) -- (c3) node[midway, above] {\footnotesize $g$};
    % g∘f: A→C (orange dashed, curved over)
    \draw[arrgf] (a1.north) to node[midway, above=0.2cm] {\footnotesize $g\!\circ\!f$} (c1.north);
    \draw[arrgf] (a2) to node[midway, above=0.2cm] {\footnotesize $g\!\circ\!f$} (c2);
    \draw[arrgf] (a3.south) to node[midway, below=0.2cm] {\footnotesize $g\!\circ\!f$} (c3.south);
    % Label
    \node[font=\sffamily\small, align=center] at (4,2.8) {$(g \circ f)(x) = g(f(x))$};
  \end{tikzpicture}
\end{frame}\begin{frame}{ฟังก์ชันผกผัน (Inverse Functions)}
  \begin{explanation}[นิยาม]
    ให้ $f: A \to B$ เป็นฟังก์ชันหนึ่งต่อหนึ่งและทั่วถึง (bijective)

    \medskip
    \textbf{ฟังก์ชันผกผัน} $f^{-1}: B \to A$ คือฟังก์ชันที่

    \medskip
    $f^{-1}(y) = x$ ก็ต่อเมื่อ $f(x) = y$

    \medskip
    \textbf{สมบัติสำคัญ:}
    \begin{itemize}
      \item $(f^{-1} \circ f)(x) = x$ สำหรับทุก $x \in A$
      \item $(f \circ f^{-1})(y) = y$ สำหรับทุก $y \in B$
    \end{itemize}
  \end{explanation}

  \begin{intuition}[สังเกต]
    ง่าย ๆ คือ สลับฝั่ง $x$ และ $y$ จาก $(x, y)$ เป็น $(y, x)$
  \end{intuition}
\end{frame}\begin{frame}{แผนภาพ: ฟังก์ชันผกผัน}
  \begin{columns}[T]
    \column{0.5\textwidth}
      \centering
      \begin{tikzpicture}[scale=0.85,
        elem/.style={circle, draw, thick, fill=white, inner sep=0pt, minimum size=18pt, font=\sffamily},
        arrf/.style={->, >=stealth, very thick, boxBlue, shorten >=2pt, shorten <=2pt},
        arrinv/.style={->, >=stealth, very thick, boxOrange, shorten >=2pt, shorten <=2pt}]
        % Set A (left)
        \draw[boxBlue!50, very thick] (0,0) ellipse (1.2cm and 2.0cm);
        \node[font=\sffamily\small\bfseries] at (0,-2.6) {เซต $A$};
        % Set B (right)
        \draw[boxBlue!50, very thick] (4.5,0) ellipse (1.2cm and 2.0cm);
        \node[font=\sffamily\small\bfseries] at (4.5,-2.6) {เซต $B$};
        % Elements
        \node[elem] (a1) at (0,1.0) {1};
        \node[elem] (a2) at (0,0.0) {2};
        \node[elem] (a3) at (0,-1.0) {3};
        \node[elem] (b1) at (4.5,1.0) {4};
        \node[elem] (b2) at (4.5,0.0) {5};
        \node[elem] (b3) at (4.5,-1.0) {6};
        % f: A→B (solid, forward)
        \draw[arrf] (a1) -- (b1) node[midway, above] {\footnotesize $f$};
        \draw[arrf] (a2) -- (b3) node[midway, above] {\footnotesize $f$};
        \draw[arrf] (a3) -- (b2) node[midway, above] {\footnotesize $f$};
        % f⁻¹: B→A (dashed, backward)
        \draw[arrinv, dashed] (b1) -- (a1) node[midway, below] {\footnotesize $f^{-1}$};
        \draw[arrinv, dashed] (b3) -- (a2) node[midway, below] {\footnotesize $f^{-1}$};
        \draw[arrinv, dashed] (b2) -- (a3) node[midway, below] {\footnotesize $f^{-1}$};
        % Center note
        \node[font=\sffamily\small, align=center, boxBlue] at (2.25,2.5)
          {$f(1)=4 \iff f^{-1}(4)=1$};
      \end{tikzpicture}

    \column{0.5\textwidth}
      \begin{intuition}[หลักการ]
        \begin{itemize}
          \item $f$ (นํ้าเงิน): ฟังก์ชันตรง จาก $A$ ไป $B$
          \item $f^{-1}$ (ส้ม): ฟังก์ชันผกผัน จาก $B$ กลับ $A$
          \item \highlight{สลับฝั่ง} $x$ และ $y$: $(x,y) \to (y,x)$
          \item $f$ ต้องเป็น \textbf{bijective} เท่านั้น จึงจะมี $f^{-1}$ ได้
        \end{itemize}
      \end{intuition}
  \end{columns}
\end{frame}\begin{frame}[t]{ตัวอย่าง: การหาฟังก์ชันผกผัน}
  \begin{exambox}[โจทย์]
    กำหนด $f(x) = 2x - 5$ จงหา $f^{-1}(x)$
  \end{exambox}

  \vspace{\fill}

\end{frame}\begin{frame}[t]{ตัวอย่าง: ข้อสอบจริง}
  \begin{exambox}[โจทย์ปี 67 ข้อ 5]
    สำหรับจำนวนจริง $x>1$ กำหนด $f(x)=2x+1$ และ $g(x)=x^2$ จงหาค่าของ $(f\circ g)^{-1}(x)$
  \end{exambox}

  \vspace{\fill}

\end{frame}

% ===========================================================================
%  SECTION: คุณสมบัติของความสัมพันธ์ (Properties of Relations)
% ===========================================================================
\section{คุณสมบัติของความสัมพันธ์}

\begin{frame}{สมบัติสะท้อน (Reflexive)}
  \begin{explanation}[นิยาม]
    ความสัมพันธ์ $r$ บนเซต $A$ มีสมบัติ \highlight{สะท้อน} (Reflexive) ก็ต่อเมื่อ

    \medskip
    \textbf{ทุกสมาชิกใน $A$ สัมพันธ์กับตัวเอง}: $(a, a) \in r$ สำหรับทุก $a \in A$
  \end{explanation}

  \begin{exambox}[ตัวอย่าง]
    $A = \{1, 2, 3\}$,
    $r = \{(1,1), (2,2), (3,3), (1,2), (2,3)\}$
    \par $\rightarrow$ reflexive เพราะมี $(1,1), (2,2), (3,3)$ ครบทุกตัว
  \end{exambox}
\end{frame}\begin{frame}{สมบัติสะท้อน (ต่อ)}
  \begin{exambox}[ตัวอย่างที่ไม่ใช่]
    $r = \{(1,1), (2,2), (1,2)\}$ บน $A = \{1, 2, 3\}$
    \par $\rightarrow$ \textbf{ไม่} reflexive เพราะขาด $(3,3)$
  \end{exambox}

  \begin{intuition}[คิดง่าย ๆ]
    สะท้อน = ``ทุกคนรู้จักตัวเอง''

    ทุกสมาชิกต้องมีลูกศรชี้กลับมาหาตัวเองครบทุกตัว
    ถ้าขาดแม้แต่ตัวเดียว จะไม่ใช่ reflexive ทันที
  \end{intuition}
\end{frame}\begin{frame}{แผนภาพ: สมบัติสะท้อน}
  \begin{columns}[T]
    \column{0.48\textwidth}
      \centering
      \textbf{สะท้อน $\checkmark$}\\[0.3em]
      \begin{tikzpicture}[scale=0.85,
        elem/.style={circle, draw, thick, fill=white, inner sep=0pt, minimum size=18pt, font=\sffamily},
        arrg/.style={->, >=stealth, very thick, boxGreen, shorten >=2pt, shorten <=2pt}]
        % Left A
        \draw[boxGreen!40, very thick] (0,0) ellipse (1.1cm and 2.0cm);
        \node[font=\sffamily\small] at (0,-2.5) {$A$};
        % Right A
        \draw[boxGreen!40, very thick] (3.5,0) ellipse (1.1cm and 2.0cm);
        \node[font=\sffamily\small] at (3.5,-2.5) {$A$};
        % Elements
        \node[elem] at (0,0.9) {1};
        \node[elem] at (0,0.0) {2};
        \node[elem] at (0,-0.9) {3};
        \node[elem] at (3.5,0.9) {1};
        \node[elem] at (3.5,0.0) {2};
        \node[elem] at (3.5,-0.9) {3};
        % Every element maps to itself
        \draw[arrg] (0.2,0.9) -- (3.3,0.9) node[midway, above] {\tiny $1\!\to\!1$};
        \draw[arrg] (0.2,0.0) -- (3.3,0.0) node[midway, above] {\tiny $2\!\to\!2$};
        \draw[arrg] (0.2,-0.9) -- (3.3,-0.9) node[midway, above] {\tiny $3\!\to\!3$};
        \node[boxGreen, font=\sffamily\footnotesize] at (1.75,-3.2)
          {ทุกตัวมีลูกศรชี้กลับหาตัวเอง};
      \end{tikzpicture}

    \column{0.48\textwidth}
      \centering
      \textbf{ไม่สะท้อน $\times$}\\[0.3em]
      \begin{tikzpicture}[scale=0.85,
        elem/.style={circle, draw, thick, fill=white, inner sep=0pt, minimum size=18pt, font=\sffamily},
        elembad/.style={circle, draw=red!70, thick, fill=red!10, inner sep=0pt, minimum size=18pt, font=\sffamily},
        arrr/.style={->, >=stealth, very thick, red!70, shorten >=2pt, shorten <=2pt},
        arrd/.style={->, >=stealth, thick, red!30, dashed, shorten >=2pt, shorten <=2pt}]
        \draw[red!30, very thick] (0,0) ellipse (1.1cm and 2.0cm);
        \node[font=\sffamily\small] at (0,-2.5) {$A$};
        \draw[red!30, very thick] (3.5,0) ellipse (1.1cm and 2.0cm);
        \node[font=\sffamily\small] at (3.5,-2.5) {$A$};
        \node[elem] at (0,0.9) {1};
        \node[elem] at (0,0.0) {2};
        \node[elembad] at (0,-0.9) {3};
        \node[elem] at (3.5,0.9) {1};
        \node[elem] at (3.5,0.0) {2};
        \node[elembad] at (3.5,-0.9) {3};
        \draw[arrr] (0.2,0.9) -- (3.3,0.9);
        \draw[arrr] (0.2,0.0) -- (3.3,0.0);
        \draw[arrd] (0.2,-0.9) -- (3.3,-0.9);
        \draw[arrr] (0.2,-0.9) -- (3.3,-0.2); % 3→2 instead of 3→3
        \node[red, font=\sffamily\footnotesize] at (1.75,-3.2)
          {ขาด $3\!\to\!3$! $\rightarrow$ ไม่สะท้อน};
      \end{tikzpicture}
  \end{columns}
\end{frame}\begin{frame}{สมบัติสมมาตร (Symmetric)}
  \begin{explanation}[นิยาม]
    ความสัมพันธ์ $r$ บนเซต $A$ มีสมบัติ \highlight{สมมาตร} (Symmetric) ก็ต่อเมื่อ

    \medskip
    \textbf{ถ้า $a$ สัมพันธ์กับ $b$ แล้ว $b$ จะสัมพันธ์กับ $a$ ด้วย}:

    \medskip
    ถ้า $(a, b) \in r$ แล้ว $(b, a) \in r$
  \end{explanation}

  \begin{exambox}[ตัวอย่าง]
    $r = \{(1,2), (2,1), (1,3), (3,1)\}$
    \par $\rightarrow$ symmetric: ทุกคู่มีคู่สลับใน $r$
  \end{exambox}
\end{frame}\begin{frame}{สมบัติสมมาตร (ต่อ)}
  \begin{exambox}[ตัวอย่างที่ไม่ใช่]
    $r = \{(1,2), (2,3)\}$
    \par $\rightarrow$ \textbf{ไม่} symmetric เพราะมี $(1,2)$ แต่ไม่มี $(2,1)$
  \end{exambox}

  \begin{intuition}[คิดง่าย ๆ]
    สมมาตร = ``รู้จักกันทั้งสองฝ่าย''

    ถ้ามีลูกศรจาก $a$ ไป $b$ ต้องมีลูกศรย้อนกลับจาก $b$ ไป $a$ ด้วย
    ถ้าขาดแม้แต่คู่เดียว จะไม่ใช่ symmetric
  \end{intuition}
\end{frame}\begin{frame}{แผนภาพ: สมบัติสมมาตร}
  \begin{columns}[T]
    \column{0.48\textwidth}
      \centering
      \textbf{สมมาตร $\checkmark$}\\[0.3em]
      \begin{tikzpicture}[scale=0.85,
        elem/.style={circle, draw, thick, fill=white, inner sep=0pt, minimum size=18pt, font=\sffamily},
        arrg/.style={->, >=stealth, very thick, boxGreen, shorten >=2pt, shorten <=2pt}]
        \draw[boxGreen!40, very thick] (0,0) ellipse (1.1cm and 2.0cm);
        \node[font=\sffamily\small] at (0,-2.5) {$A$};
        \draw[boxGreen!40, very thick] (3.5,0) ellipse (1.1cm and 2.0cm);
        \node[font=\sffamily\small] at (3.5,-2.5) {$A$};
        \node[elem] at (0,1.0) {1};
        \node[elem] at (0,-1.0) {2};
        \node[elem] at (3.5,1.0) {1};
        \node[elem] at (3.5,-1.0) {2};
        % Both directions as separate left→right arrows
        \draw[arrg] (0.2,1.0) -- (3.3,-1.0)
          node[midway, above, font=\sffamily\tiny] {$(1,2)$};
        \draw[arrg] (0.2,-1.0) -- (3.3,1.0)
          node[midway, below, font=\sffamily\tiny] {$(2,1)$};
        \node[boxGreen, font=\sffamily\footnotesize] at (1.75,-2.8)
          {มี $(1,2)$ \textbf{และ} $(2,1)$ ครบคู่};
      \end{tikzpicture}

    \column{0.48\textwidth}
      \centering
      \textbf{ไม่สมมาตร $\times$}\\[0.3em]
      \begin{tikzpicture}[scale=0.85,
        elem/.style={circle, draw, thick, fill=white, inner sep=0pt, minimum size=18pt, font=\sffamily},
        arrr/.style={->, >=stealth, very thick, red!70, shorten >=2pt, shorten <=2pt},
        arrd/.style={->, >=stealth, thick, red!30, dashed, shorten >=2pt, shorten <=2pt}]
        \draw[red!30, very thick] (0,0) ellipse (1.1cm and 2.0cm);
        \node[font=\sffamily\small] at (0,-2.5) {$A$};
        \draw[red!30, very thick] (3.5,0) ellipse (1.1cm and 2.0cm);
        \node[font=\sffamily\small] at (3.5,-2.5) {$A$};
        \node[elem] at (0,1.0) {1};
        \node[elem] at (0,-1.0) {2};
        \node[elem] at (3.5,1.0) {1};
        \node[elem] at (3.5,-1.0) {2};
        % (1,2) present
        \draw[arrr] (0.2,1.0) -- (3.3,-1.0)
          node[midway, above, font=\sffamily\tiny] {$(1,2)$};
        % (2,1) MISSING — dashed, still left→right
        \draw[arrd] (0.2,-1.0) -- (3.3,1.0)
          node[midway, below, font=\sffamily\tiny, red] {ไม่มี $(2,1)$};
        \node[red, font=\sffamily\footnotesize] at (1.75,-2.8)
          {มี $(1,2)$ แต่\textbf{ขาด} $(2,1)$!};
      \end{tikzpicture}
  \end{columns}
\end{frame}\begin{frame}{สมบัติถ่ายทอด (Transitive)}
  \begin{explanation}[นิยาม]
    ความสัมพันธ์ $r$ บนเซต $A$ มีสมบัติ \highlight{ถ่ายทอด} (Transitive) ก็ต่อเมื่อ

    \medskip
    \textbf{ถ้า $a$ สัมพันธ์กับ $b$ และ $b$ สัมพันธ์กับ $c$ แล้ว $a$ จะสัมพันธ์กับ $c$}:

    \medskip
    ถ้า $(a, b) \in r$ และ $(b, c) \in r$ แล้ว $(a, c) \in r$
  \end{explanation}

  \begin{exambox}[ตัวอย่าง]
    $r = \{(1,2), (2,3), (1,3), (3,4), (2,4), (1,4)\}$
    \par $\rightarrow$ transitive: ทุกกรณีที่มี $(a,b)$ และ $(b,c)$ จะต้องมี $(a,c)$ ด้วย
  \end{exambox}
\end{frame}\begin{frame}{แผนภาพ: สมบัติถ่ายทอด}
  \begin{columns}[T]
    \column{0.48\textwidth}
      \centering
      \textbf{ถ่ายทอด $\checkmark$}\\[0.3em]
      \begin{tikzpicture}[scale=0.85,
        elem/.style={circle, draw, thick, fill=white, inner sep=0pt, minimum size=18pt, font=\sffamily},
        arrg/.style={->, >=stealth, very thick, boxGreen, shorten >=2pt, shorten <=2pt}]
        \draw[boxGreen!40, very thick] (0,0) ellipse (1.1cm and 2.0cm);
        \node[font=\sffamily\small] at (0,-2.5) {$A$};
        \draw[boxGreen!40, very thick] (3.5,0) ellipse (1.1cm and 2.0cm);
        \node[font=\sffamily\small] at (3.5,-2.5) {$A$};
        \node[elem] at (0,0.9) {1};
        \node[elem] at (0,0.0) {2};
        \node[elem] at (0,-0.9) {3};
        \node[elem] at (3.5,0.9) {1};
        \node[elem] at (3.5,0.0) {2};
        \node[elem] at (3.5,-0.9) {3};
        % 1→2, 2→3, AND 1→3
        \draw[arrg] (0.2,0.9) -- (3.3,0.0) node[midway, above] {\tiny $1\!\to\!2$};
        \draw[arrg] (0.2,0.0) -- (3.3,-0.9) node[midway, below] {\tiny $2\!\to\!3$};
        \draw[arrg, bend left=20] (0.2,0.9) to node[midway, below=0.2cm] {\tiny $1\!\to\!3$} (3.3,-0.9);
        \node[boxGreen, font=\sffamily\footnotesize] at (1.75,-3.2)
          {มี $1\!\to\!2$, $2\!\to\!3$ \textbf{และ} $1\!\to\!3$};
      \end{tikzpicture}

    \column{0.48\textwidth}
      \centering
      \textbf{ไม่ถ่ายทอด $\times$}\\[0.3em]
      \begin{tikzpicture}[scale=0.85,
        elem/.style={circle, draw, thick, fill=white, inner sep=0pt, minimum size=18pt, font=\sffamily},
        arrr/.style={->, >=stealth, very thick, red!70, shorten >=2pt, shorten <=2pt},
        arrd/.style={->, >=stealth, thick, red!30, dashed, shorten >=2pt, shorten <=2pt}]
        \draw[red!30, very thick] (0,0) ellipse (1.1cm and 2.0cm);
        \node[font=\sffamily\small] at (0,-2.5) {$A$};
        \draw[red!30, very thick] (3.5,0) ellipse (1.1cm and 2.0cm);
        \node[font=\sffamily\small] at (3.5,-2.5) {$A$};
        \node[elem] at (0,0.9) {1};
        \node[elem] at (0,0.0) {2};
        \node[elem] at (0,-0.9) {3};
        \node[elem] at (3.5,0.9) {1};
        \node[elem] at (3.5,0.0) {2};
        \node[elem] at (3.5,-0.9) {3};
        % 1→2, 2→3 present
        \draw[arrr] (0.2,0.9) -- (3.3,0.0);   % 1→2
        \draw[arrr] (0.2,0.0) -- (3.3,-0.9);   % 2→3
        % 1→3 MISSING
        \draw[arrd, bend left=20] (0.2,0.9) to (3.3,-0.9);
        \node[red, font=\sffamily\footnotesize] at (1.75,-3.2)
          {มี $1\!\to\!2$, $2\!\to\!3$ แต่\textbf{ขาด} $1\!\to\!3$};
      \end{tikzpicture}
  \end{columns}
\end{frame}\begin{frame}{ความสัมพันธ์สมมูล (Equivalence Relation)}
  \begin{explanation}[นิยาม]
    ความสัมพันธ์ $r$ บนเซต $A$ เป็น \highlight{ความสัมพันธ์สมมูล} (Equivalence Relation)
    ก็ต่อเมื่อ $r$ มีสมบัติครบทั้งสามข้อ:

    \medskip
    \begin{enumerate}
      \item \textbf{สะท้อน} (Reflexive)
      \item \textbf{สมมาตร} (Symmetric)
      \item \textbf{ถ่ายทอด} (Transitive)
    \end{enumerate}
  \end{explanation}
\end{frame}\begin{frame}{แผนภาพ: ความสัมพันธ์สมมูล}
  \begin{columns}[T]
    \column{0.5\textwidth}
      \centering
      \begin{tikzpicture}[scale=0.85,
        elem/.style={circle, draw=boxBlue, thick, fill=white, inner sep=0pt, minimum size=18pt, font=\sffamily},
        arr/.style={->, >=stealth, very thick, boxBlue, shorten >=2pt, shorten <=2pt}]
        % Set A (left)
        \draw[boxBlue!50, very thick] (0,0) ellipse (1.3cm and 2.3cm);
        \node[font=\sffamily\small\bfseries] at (0,-2.8) {เซต $A$};
        % Set A (right)
        \draw[boxBlue!50, very thick] (4.5,0) ellipse (1.3cm and 2.3cm);
        \node[font=\sffamily\small\bfseries] at (4.5,-2.8) {เซต $A$};
        % === {1,2,3}: all-to-all (reflexive + symmetric + transitive) ===
        \node[elem] at (0,1.5) {1};
        \node[elem] at (0,0.5) {2};
        \node[elem] at (0,-0.5) {3};
        \node[elem] at (4.5,1.5) {1};
        \node[elem] at (4.5,0.5) {2};
        \node[elem] at (4.5,-0.5) {3};
        % Self-loops (reflexive)
        \draw[arr] (0.2,1.5) -- (4.3,1.5);  % 1→1
        \draw[arr] (0.2,0.5) -- (4.3,0.5);  % 2→2
        \draw[arr] (0.2,-0.5) -- (4.3,-0.5); % 3→3
        % Bidirectional (symmetric)
        \draw[arr] (0.2,1.5) -- (4.3,0.5);  % 1→2
        \draw[arr] (0.2,0.5) -- (4.3,1.5);  % 2→1
        \draw[arr] (0.2,0.5) -- (4.3,-0.5); % 2→3
        \draw[arr] (0.2,-0.5) -- (4.3,0.5); % 3→2
        \draw[arr] (0.2,1.5) -- (4.3,-0.5); % 1→3 (transitive!)
        \draw[arr] (0.2,-0.5) -- (4.3,1.5);  % 3→1 (transitive!)
        % === {4}: just self-loop ===
        \node[elem] at (0,-1.7) {4};
        \node[elem] at (4.5,-1.7) {4};
        \draw[arr] (0.2,-1.7) -- (4.3,-1.7); % 4→4
      \end{tikzpicture}

    \column{0.45\textwidth}
      \begin{intuition}[สังเกต]
        \begin{itemize}
          \item \textbf{ทุกตัว}มีลูกศรหาตัวเอง (สะท้อน)
          \item \textbf{ทุกลูกศร}มีลูกศรย้อนกลับ (สมมาตร)
          \item $1\!\to\!2$ และ $2\!\to\!3$ $\rightarrow$ มี $1\!\to\!3$ ด้วย (ถ่ายทอด)
          \item ครบทั้งสาม $\rightarrow$ \highlight{ความสัมพันธ์สมมูล}
        \end{itemize}
      \end{intuition}
  \end{columns}
\end{frame}\begin{frame}{ตัวอย่าง: ความสัมพันธ์สมมูล}

  \begin{exambox}[ตัวอย่าง]
    ความสัมพันธ์ ``เท่ากัน'' ($=$) บนเซตของจำนวนจริง
    \begin{itemize}
      \item สะท้อน: $a = a$ เสมอ $\checkmark$
      \item สมมาตร: ถ้า $a = b$ แล้ว $b = a$ $\checkmark$
      \item ถ่ายทอด: ถ้า $a = b$ และ $b = c$ แล้ว $a = c$ $\checkmark$
    \end{itemize}
    ดังนั้น $=$ เป็น equivalence relation
  \end{exambox}
\end{frame}\begin{frame}[t]{ตัวอย่าง: ข้อสอบจริง}
  \begin{exambox}[โจทย์ปี 68 ข้อ 9]
    กำหนด $r=\{(a,b) \in A \times  A \mid 4a \leq 5b\}$ แทนความสัมพันธ์ \newline
    โดยที่ $A=\{1,2,3,4,5\}$ ให้ $m$ เป็นจำนวนสมาชิกในเซต $r$ และให้ $n$ เป็นจำนวนสมาชิกอย่างน้อยจาก $A \times A$ ที่ต้องเพิ่มเข้าไปใน $r$ ซึ่งทำให้ $r$ มีสมบัติสมมาตร จงหาว่า $m+n$ มีค่าเท่าใด
  \end{exambox}

  \vspace{\fill}

\end{frame}

% ===========================================================================
%  SUMMARY SLIDE
% ===========================================================================
\section{สรุป}

\begin{frame}{สรุปเนื้อหา}
  \begin{explanation}[สิ่งที่ได้เรียนรู้]
    \begin{enumerate}
      \item \textbf{ความสัมพันธ์} คือเซตย่อยของผลคูณคาร์ทีเซียน $A \times B$
      \item \textbf{ฟังก์ชัน} คือความสัมพันธ์ที่แต่ละ input มี output เพียงค่าเดียว
      \item \textbf{โดเมนและเรนจ์} บอกขอบเขตของ input และ output
      \item \textbf{ประเภทของฟังก์ชัน}: หนึ่งต่อหนึ่ง, ทั่วถึง, สมนัยหนึ่งต่อหนึ่ง
      \item \textbf{การดำเนินการ}: บวก ลบ คูณ หาร ประกอบ และผกผัน
      \item \textbf{สมบัติของความสัมพันธ์}: สะท้อน สมมาตร ถ่ายทอด และสมมูล
    \end{enumerate}
  \end{explanation}

  \begin{center}
    \large เอกสารนี้เป็นส่วนหนึ่งของเนื้อหาเตรียมสอบ \textbf{สอวน. คอมพิวเตอร์}
  \end{center}
\end{frame}

\end{document}
